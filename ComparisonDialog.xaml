<Window x:Class="Hephaistos.ComparisonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Comparer deux PDF"
        Width="620"
        Height="300"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="24">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,18"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Text="Choisir les deux documents à comparer"/>

        <StackPanel Grid.Row="1"
                    Margin="0,0,0,14">

            <TextBlock Margin="0,0,0,5"
                       FontWeight="SemiBold"
                       Text="Document A"/>

            <ComboBox x:Name="DocumentAComboBox"
                      Height="34"
                      Padding="6"
                      DisplayMemberPath="DisplayText"/>

        </StackPanel>

        <StackPanel Grid.Row="2">

            <TextBlock Margin="0,0,0,5"
                       FontWeight="SemiBold"
                       Text="Document B"/>

            <ComboBox x:Name="DocumentBComboBox"
                      Height="34"
                      Padding="6"
                      DisplayMemberPath="DisplayText"/>

        </StackPanel>

        <StackPanel Grid.Row="4"
                    Margin="0,22,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Width="100"
                    Margin="0,0,10,0"
                    Padding="10,6"
                    Content="Annuler"
                    IsCancel="True"/>

            <Button Width="110"
                    Padding="10,6"
                    Content="Comparer"
                    IsDefault="True"
                    Click="CompareButton_Click"/>

        </StackPanel>

    </Grid>

</Window>